services:
  somspeak:
    build: .
    ports:
      - "5000:5000"
    env_file: .env
    volumes:
      - somspeak-data:/data
    environment:
      DATABASE_PATH: /data/somspeak.db
volumes:
  somspeak-data:
